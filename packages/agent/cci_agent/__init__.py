"""cci_agent — the Sift agent layer: Advise, Execute, Close.

Proactive features built on the v1 retrieval loop + the agent foundations
(state machine, outcome log, voice memory, offers, rules). Every drafting
feature grounds in the creator's own content and rewrites into their voice;
correctness/citations stay a retrieval concern, voice is a post-retrieval layer.
"""
